apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  namespace: default
spec:
  type: LoadBalancer
  externalIPs:
    - 198.19.249.102
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: guestbook-ui