kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: guestbook-pv-claim
  namespace: default
spec:
  storageClassName: openebs-hostpath
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 0.1Gi
